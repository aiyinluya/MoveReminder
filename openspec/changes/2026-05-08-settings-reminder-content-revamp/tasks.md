# 任务清单：2026-05-08-settings-reminder-content-revamp

- [x] 撰写 proposal / design（本目录）
- [x] 更新 `openspec/specs/move-reminder/spec.md`（FR-8）与 `test-plan.md`（AC-11）
- [x] `TextColorPickerSection`：推荐色行固定高度，避免被父布局压缩
- [x] `SettingsForm`：单选切换文字/图片；图片区路径+浏览一行；预览与历史并列；移除「保存后生效」及旧 Tab/绿条
- [x] `dotnet build -c Release`（若本机 exe 占用则 `Debug`）验证

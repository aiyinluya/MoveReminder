# 任务清单 — improve-ui-polish

- [x] 新增 `Assets/app.ico` 并配置 `ApplicationIcon`
- [x] `Program` 高 DPI + 视觉样式
- [x] `UiTheme`、`AboutForm`、托盘菜单与推迟/跳过逻辑
- [x] `SettingsForm` 品牌化布局
- [x] `ReminderForm` 按钮与层次微调
- [x] 更新 `openspec/specs/move-reminder/spec.md` / `test-plan.md`
- [x] `dotnet publish` 输出到 `artifacts/MoveReminder-publish`

# 任务清单：设置三模块 + 颜色 UX

- [x] `ReminderTextColorHelper`：增加与 `Presets` 对齐的 `PresetHints`。
- [x] `ColorSwatchControl`：圆形色块、选中环、`DoubleBuffered` 绘制。
- [x] `TextColorPickerSection`：预览、HEX、说明、系统调色盘、推荐色行、ToolTip、`FindForm` 挂接 `ColorDialog`。
- [x] `SettingsForm`：三卡片布局、常规表拆出「提醒方式」、文字/图片 body 表、`UpdateModeUi` 按模块 `Enabled`、`OnLoadSyncLayout` 宽度同步。
- [x] `openspec/specs/move-reminder/spec.md`：FR-8 与相关段落更新。
- [x] `openspec/specs/move-reminder/test-plan.md`：增加模块与颜色 UI 手工项。
- [x] `openspec/project.md`：版本与最近更新表。
- [x] `dotnet build -c Release` 通过。
